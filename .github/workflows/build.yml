
name: build

on:
  push:
    branches: [ "main", "xattr" ]
  pull_request:
    branches: [ "main" ]

jobs:
  build:

    runs-on: macos-12

    steps:
    - name: checkout
      uses: actions/checkout@v3

    - name: build
      run: swift build -v
